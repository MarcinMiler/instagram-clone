type Response {
    ok: Boolean!
    error: String
}

type Query {
    hello: String
}

type Mutation {
    register(
        email: String!
        password: String!
        username: String!
        fullname: String!
    ): Response!
}
